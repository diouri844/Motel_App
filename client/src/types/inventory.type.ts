
interface InventoryItem {
    // Define the structure of your inventory item (e.g., id, name, etc.)
    id: string;
    name: string;
    description: string;
    createdAt: string;
    userId: string;
    // ... other properties
}


export default InventoryItem;